{% extends 'app/base.html.twig' %}

{% block toolbar %}
    {{ component('Crud:Toolbar', {
        path: 'app_events_',
        entity: event,
        back: true,
        delete: true,
    })  }}
{% endblock %}

{% block body %}
    {{ component('Event:Form', {
        isEditing: true,
        entity: event,
    }) }}
{% endblock %}