{% if this.value is iterable %}
    {% for item in this.value %}
        <span class="badge rounded bg-info me-2">{{ item ? 'Yes' : 'No' }}
    {% endfor %}
{% else %}
    {{ this.value ? 'Yes' : 'No' }}
{% endif %}