{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Brands{% endblock %}
{# 
    {% set items = brands %} 
#}

{% block body %}
    <div class="container">
        Auf dieser Seite findest du "Brands".

        {# 
            {% for item in items %}
                <div class="card">{{ item.name }}</div>
            {% endfor %}
        #}
        
    </div>
{% endblock %}