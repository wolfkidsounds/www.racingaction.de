{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Tracks{% endblock %}
{# 
    {% set items = tracks %} 
#}

{% block body %}
    <div class="container">
        Auf dieser Seite findest du "Tracks".

        {# 
            {% for item in items %}
                <div class="card">{{ item.name }}</div>
            {% endfor %}
        #}
        
    </div>
{% endblock %}