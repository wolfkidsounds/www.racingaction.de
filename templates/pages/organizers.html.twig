{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Organizers{% endblock %}
{# 
    {% set items = organizers %} 
#}

{% block body %}
    <div class="container">
        Auf dieser Seite findest du "Organizers".

        {# 
            {% for item in items %}
                <div class="card">{{ item.name }}</div>
            {% endfor %}
        #}
        
    </div>
{% endblock %}